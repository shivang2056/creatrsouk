<div class="w-[850px] mx-auto bg-white shadow-xl rounded-xl p-12">
  <div class="flex h-60 mb-16">
    <%= image_tag @product.image, class: "rounded-md object-cover object-center filter saturate-50", alt: @product.name %>

    <div class="flex flex-col pl-4 justify-between">
      <div class="flex flex-col">
        <div class="flex justify-between">
          <h1 class="text-3xl font-medium text-gray-900 "> <%= @product.name %> </h1>
          <%= link_to "View Receipt", @receipt_url, target: "_blank", class: "text-sm duration-200 border font-medium focus:outline-none focus:ring-2 inline-flex items-center whitespace-nowrap bg-gray-800 hover:bg-gray-900 text-white justify-center rounded-md shadow-sm border-transparent h-10 py-2 px-4"%>
        </div>
        <div class="text-sm text-gray-300 pl-1 mb-4">
          By <%= @product.user.full_name %>
        </div>
      </div>

      <div class="text-sm text-gray-500 pl-1 line-clamp-6">
        <%= @product.description %>
      </div>
    </div>
  </div>

  <div class="border-t-2">
    <div class="text-gray-700 py-4 text-sm">
      You have successfully purchased the product, '<span class="text-gray-900"><%= @product.name %></span>'. Please find the attachments related to your purchase below.
    </div>

    <% @product.attachments.each do |attachment| %>
      <%= render "attachments/card", attachment: attachment, product: @product, show_delete_link: false %>
    <% end %>
  </div>
</div>